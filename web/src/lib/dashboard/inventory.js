import { getSupabaseClient } from '../supabase/client'

/**
 * Fetch low stock alerts or batch expiration (placeholder)
 */
export async function getInventoryAlerts() {
  // Future implementation
  return []
}
